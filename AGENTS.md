# AGENTS.md

The canonical agent instructions for this repository live in
**[`CLAUDE.md`](CLAUDE.md)** — read that first. It covers what this project is,
the build / test / run commands, the directory map, the conventions, and the git
workflow.

This `AGENTS.md` is a pointer, not a second source of truth: it exists so that
agents which look for `AGENTS.md` by convention land on the same canonical doc.
There are no separate rules here to drift out of sync — if another tool's
expectations and `CLAUDE.md` ever disagree, **`CLAUDE.md` wins**.

> **CodeFlow** — single-file, zero-build browser SPA that turns a repo / PR / folder into an interactive architecture map.
